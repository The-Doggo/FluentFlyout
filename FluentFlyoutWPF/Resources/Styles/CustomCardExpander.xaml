<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:controls="clr-namespace:MicaWPF.Controls;assembly=MicaWPF">
    
    <Style x:Key="WinUi3ExpanderStyle" TargetType="{x:Type ui:CardExpander}" BasedOn="{StaticResource {x:Type ui:CardExpander}}">
        <Setter Property="ContentPadding" Value="0" />
        <Setter Property="Margin" Value="0,0,0,12" />
        <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorDefaultBrush}" />
        <Style.Resources>
            <SolidColorBrush x:Key="CardBackground" Color="Transparent" />
        </Style.Resources>
    </Style>

    <Style x:Key="WinUi3ExpanderToggleSwitchStyle" TargetType="{x:Type controls:ToggleSwitch}" BasedOn="{StaticResource {x:Type controls:ToggleSwitch}}">
        <Setter Property="Margin" Value="14,0,14,0" />
        <Setter Property="HorizontalAlignment" Value="Right" />
    </Style>

    <Style x:Key="WinUi3ExpanderComboBoxStyle" TargetType="{x:Type ComboBox}" BasedOn="{StaticResource {x:Type ComboBox}}">
        <Setter Property="Margin" Value="14,0,14,0" />
        <Setter Property="HorizontalAlignment" Value="Right" />
    </Style>

    <Style x:Key="WinUi3ExpanderButtonStyle" TargetType="{x:Type ui:Button}" BasedOn="{StaticResource {x:Type ui:Button}}">
        <Setter Property="Margin" Value="14,0,14,0" />
        <Setter Property="HorizontalAlignment" Value="Right" />
    </Style>

    <Style x:Key="WinUi3ExpanderCardControlStyle" TargetType="{x:Type ui:CardControl}" BasedOn="{StaticResource {x:Type ui:CardControl}}">
        <!-- Make sure to remove margins/padding of child elements -->
        <Setter Property="Margin" Value="0" />
        <Setter Property="Padding" Value="58,10,44,10" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorDefaultBrush}" />
        <Setter Property="CornerRadius" Value="0" />
    </Style>

    <Style x:Key="WinUi3ExpanderInfoBarStyle" TargetType="{x:Type ui:InfoBar}" BasedOn="{StaticResource {x:Type ui:InfoBar}}">
        <!-- Make sure to remove margins/padding of child elements -->
        <Setter Property="Margin" Value="0" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="Background" Value="{DynamicResource ControlFillColorTertiaryBrush}" />
        <Setter Property="Border.CornerRadius" Value="0" />
    </Style>

    <Style x:Key="WinUi3ExpanderCardControlBottomStyle" TargetType="{x:Type ui:CardControl}" BasedOn="{StaticResource WinUi3ExpanderCardControlStyle}">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="0,0,4,4" />
    </Style>

    <Style x:Key="WinUi3ExpanderInfoBarBottomStyle" TargetType="{x:Type ui:InfoBar}" BasedOn="{StaticResource WinUi3ExpanderInfoBarStyle}">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Border.CornerRadius" Value="0,0,4,4" />
    </Style>

</ResourceDictionary>